// DEPRECATED — replaced by services/lookupApi.ts as part of the
// DirectoryService -> LookupService rename. Kept only so nothing breaks if
// something still imports it. Safe to delete once you've confirmed no other
// references remain.
export * from "./lookupApi";
