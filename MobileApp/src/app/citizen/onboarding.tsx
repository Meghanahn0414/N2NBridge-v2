// Citizen-path alias for the onboarding screen.
// When the browser URL is /citizen/onboarding (via Vite proxy) Expo Router
// routes here.  We just re-export the real onboarding component.
export { default } from "../onboarding";
