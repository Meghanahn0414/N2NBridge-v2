server {
    listen 80;
    server_name CHANGE_ME;   # your VPS IP, or a domain once you have one

    root /var/www/n2n-frontend/current;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;   # SPA fallback for client-side routes
    }
}
